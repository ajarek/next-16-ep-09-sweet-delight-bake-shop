

generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model BakedGood {
  id        Int     @id @default(autoincrement())
  name     String
  price   Float
  category String
  image String
  description String
  quantity Int
}